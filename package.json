{
  "name": "music-electron",
  "version": "1.0.0",
  "description": "music client with electron",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron main.js",
    "build": "electron-packager . music --ignore=node_modules/electron-* --platform=linux --arch=x64 --version=0.36.10 --ignore=node_modules/.bin --ignore=.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^5.2.1",
    "electron-prebuilt": "^0.36.10"
  },
  "dependencies": {
    "fs": "0.0.2"
  }
}
